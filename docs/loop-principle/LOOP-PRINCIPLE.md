# Loop Principle

For OmniDictate, the loop is:

`orient -> plan -> act -> verify -> fix -> check -> record`

The practical version is `docs/ai/LOOP.md`. The gate list is
`docs/ai/VERIFY.md`.

The core rule is simple: do not claim a model route, UI flow, package, or
release is done until an objective gate proves it.
